import pandas as pd\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.metrics import classification_report, confusion_matrix\n\nclass ChurnModel:\n    def __init__(self, data_path):\n        self.data_path = data_path\n        self.model = None\n\n    def load_data(self):\n        self.data = pd.read_csv(self.data_path)\n        return self.data\n\n    def preprocess_data(self):\n        # Implement data preprocessing steps here\n        self.data.fillna(0, inplace=True)  # Example: fill missing values\n        self.features = self.data.drop('churn', axis=1)\n        self.labels = self.data['churn']\n        return self.features, self.labels\n\n    def train_model(self, test_size=0.2):\n        X_train, X_test, y_train, y_test = train_test_split(self.features, self.labels, test_size=test_size)\n        self.model = RandomForestClassifier()\n        self.model.fit(X_train, y_train)\n        self.evaluate_model(X_test, y_test)\n\n    def evaluate_model(self, X_test, y_test):\n        predictions = self.model.predict(X_test)\n        print(confusion_matrix(y_test, predictions))\n        print(classification_report(y_test, predictions))\n\n    def predict(self, new_data):\n        return self.model.predict(new_data)\n\n# Example usage:\n# churn_model = ChurnModel('path_to_your_data.csv')\n# churn_model.load_data()\n# churn_model.preprocess_data()\n# churn_model.train_model()\n# predictions = churn_model.predict(new_data)\n